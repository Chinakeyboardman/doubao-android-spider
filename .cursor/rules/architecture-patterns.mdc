---
description: 跨语言边界、通用设计模式、分布式任务与异步调用约定
globs: "**/*.{py,md,json,yml,yaml,sh}"
alwaysApply: false
---

# 架构、模式与跨栈约定

> **定位**：当前仓库主体为 **单机 Python + uiautomator2 真机自动化**（`app/`、`run.py`）。下列「分布式 / 多语言」条款适用于 **未来拆分** 或 **并列工具**（如 `capture/`、mitm 脚本、运维脚本）。**微服务细则**见 **`microservices.mdc`**；**DDD 与领域模型**见 **`ddd-domain.mdc`**。

## 跨语言架构规范

- **边界清晰**：Python 与其它语言（Shell、Node、Go 等）之间只通过 **明确契约** 交互：
  - **CLI**：参数名、退出码（0 成功 / 非 0 失败）、stderr 打错误信息；
  - **数据**：优先 **JSON + UTF-8**，字段名 `snake_case`，版本字段建议 `schema_version` 或 API 路径版本；
  - **路径**：不硬编码绝对路径；通过环境变量或单一配置文件注入。
- **禁止**在业务层隐式依赖「对方进程当前工作目录」；工作目录须在脚本/README 中写明。
- 文档：跨语言接口在 `doc/` 用 **一页以内** 说明输入输出示例即可（与 `doubao-spider.mdc` 文档原则一致）。

## 设计模式（贴合本项目）

- **外观（Facade）**：设备与 App 操作经 `DeviceManager`、`DoubaoApp` 等统一入口，避免在深层逻辑里散落 `device.shell` / 裸 XPath。
- **策略（Strategy）**：多套选择器、多套抓取路径用「列表顺序尝试」或策略函数，而不是巨型 `if-elif`；与 `doubao-spider.mdc` 中多策略选择器一致。
- **组合优于继承**：流程类（如 Spider、Worker）**组合**各 Handler，避免 3 层以上继承树。
- **单一职责**：`modules/` 下单文件职责清晰；单文件过长（如 >500 行）按职责拆子模块（与项目文档中的目录约定一致）。
- **依赖方向**：业务模块依赖 `config` / `utils`，避免循环 import；新增公共类型可放 `app/types.py` 或各模块 `types` 小节（若未来引入）。与 **DDD 分层、限界上下文** 对齐时见 **`ddd-domain.mdc`**。

## 分布式与任务规范（仅在队列/多机时适用）

- **任务幂等**：同一 `task_id` 重试不应产生重复副作用（重复写库、重复下单等需去重键）。
- **投递语义**：默认按 **at-least-once** 假设实现消费者（重复消息可安全重试或幂等）。
- **超时与重试**：网络调用设置 **连接/读超时**；重试带 **退避 + 上限**，并对非幂等操作谨慎重试。
- **时钟**：跨节点逻辑避免依赖本地时间一致性；优先服务端时间或单调任务序号。
- **数据一致性**：需要多步写操作时，优先「可补偿」或明确文档说明接受最终一致；不必强行上 Saga，除非业务要求。

## 异步任务规范（Python 侧）

- **uiautomator2 / adb 调用多为阻塞 I/O**：在 **`async def`** 中调用时，须使用 **`asyncio.to_thread(...)`** 或 **`loop.run_in_executor`**，禁止在事件循环线程里长时间阻塞。
- 若引入 **Celery / RQ / ARQ** 等：**任务入参出参可 JSON 序列化**；大文件传路径或对象存储 key，不传巨型内存结构。
- **并发上限**：对真机操作加 **信号量或队列**，避免多协程同时抢同一 `device`。
- 纯 CPU 密集与 I/O 分离：计算密集不要用默认线程池硬顶，必要时 `ProcessPoolExecutor`（注意序列化成本）。

## 与现有规则的关系

- 日常 Python 格式、缩进、`for` 深度、入参出参严格模式见 **`code-style.mdc`**。
- 豆包 UI、中文注释、选择器优先级见 **`doubao-spider.mdc`**。
- **微服务**（拆分、契约、数据归属、弹性、可观测性）见 **`microservices.mdc`**。
- **DDD**（限界上下文、聚合、防腐层、分层）见 **`ddd-domain.mdc`**。
