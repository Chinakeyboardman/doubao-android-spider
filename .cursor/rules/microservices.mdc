---
description: 微服务拆分、接口契约、数据归属、弹性与部署约定（扩展时适用）
globs: "**/*.{py,md,json,yml,yaml}"
alwaysApply: false
---

# 微服务设计规范

> **适用范围**：将调度、抓包、爬虫执行、结果落库等拆为 **多个独立部署单元** 时遵循。当前单体（`app/` + `run.py`）**不必**强行微服务化；本文件用于指导未来演进，避免拆完再返工。

## 服务拆分原则

- **按业务能力切分**：优先按 **业务边界**（如「任务调度」「真机执行」「流量采集」「结果归一」）而非仅按技术层（Controller/DAO）切分。
- **高内聚、低耦合**：单个服务内修改应能 **独立发布**；跨服务仅通过 **稳定契约** 通信。
- **数据归属唯一**：每个 **聚合的业务数据** 有明确 **唯一写入方**（Single Writer）；其它服务只读副本或通过 API 查询，避免双写。
- **避免分布式单体**：禁止大量服务互相直连形成网状依赖；共享库仅放 **纯函数/DTO/常量**，不放可变业务状态机。

## 接口与契约

- **同步调用**：优先 **HTTP/gRPC + 显式版本**（URL 路径或 package 版本）；**破坏性变更**须升主版本并保留兼容期或适配层。
- **文档**：对外 API 须有 **OpenAPI / Proto** 或等价说明；错误码、超时、分页字段统一约定。
- **异步消息**：事件名与载荷 **版本化**（如 `schema_version`）；消费者须 **向前兼容** 未知字段（忽略策略）。
- **幂等与去重**：写类操作支持 **Idempotency-Key** 或业务幂等键；消息消费按 **at-least-once** 设计。

## 数据与一致性

- **数据库 per service**（理想态）：服务之间 **不共享库表**；跨服务数据通过 API 或事件最终一致。
- **跨服务事务**：避免分布式两阶段事务作为默认方案；优先 **Saga / 补偿 / 最终一致** 并在文档中写明业务容忍度。
- **读模型**：可接受 **CQRS 式** 读副本或投影，但须定义 **刷新延迟** 与 **不一致时的产品行为**。

## 弹性与运行时

- **无状态进程**：会话与任务状态外置（Redis/DB/队列）；实例可随时扩缩。
- **超时**：所有出站调用设置 **连接 + 读超时**；与上游 SLA 对齐。
- **重试**：仅对 **幂等** 或 **安全可重试** 的操作自动重试；**指数退避 + 上限**；记录 `request_id` / `trace_id`。
- **熔断与限流**：保护下游与自身；限流策略在网关或服务入口统一文档化。
- **降级**：关键路径定义 **降级行为**（跳过非核心步骤、返回缓存等），避免级联故障。

## 可观测性与运维

- **结构化日志**：JSON 或键值对；必带 **trace/request/task** 关联 ID。
- **指标**：QPS、延迟分位、错误率、队列堆积、业务成功量（按域定义）。
- **健康检查**：**liveness**（进程活着）与 **readiness**（依赖可用）分离。
- **配置与密钥**：**十二因子**：配置走环境变量或密钥管理服务，**不进镜像**、不进仓库。

## 与本仓库的衔接

- 演进路径建议：**monorepo 多包 / 子进程** → 稳定后再拆独立部署；参见 `architecture-patterns.mdc` 中跨语言与异步条款。
- **领域边界** 与聚合划分见 **`ddd-domain.mdc`**，服务边界应尽量与 **限界上下文** 对齐。
