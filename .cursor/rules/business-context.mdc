---
description: 理解业务需求时须先查阅 doc/ 与既有代码，避免臆测
globs: "**/*"
alwaysApply: true
---

# 业务需求与上下文来源

## 上下文怎么用（不必整仓进对话）

- **默认依赖 `.cursor/rules/`**：业务与项目约定以规则为准，**不要**假设「整个仓库已在上下文中」；也无需把全部文件手动 @ 进来。
- **按需检索**：涉及流程/术语时，用搜索或点名打开 **根目录 `README.md`**（全流程图与入口）、**`doc/` 下与主题相关的 md**、以及 **`app/modules/` / 入口脚本** 中实际会改到的文件即可。
- **整仓塞进上下文通常不是好做法**：噪声大、易撞 token 上限、且容易覆盖真正相关的片段；**规则 + 定向读文件/搜索** 更高效。

## 何时必须查阅

当任务涉及 **业务含义、流程步骤、与豆包/商品采集相关的预期行为**（而非纯语法或重命名）时：

1. **先看全貌（可选但推荐）**：根目录 **`README.md`** — 爬虫主流程图、推荐入口 `run_flow_crawl.py`、产出目录约定。
2. **再读专题 `doc/`**：打开或检索与主题相关的 Markdown（如 `real_flow_analysis.md`、商品卡片与列表详情等），建立与仓库一致的术语与流程。
3. **再查代码**：用搜索/阅读 **历史已实现模块**（`app/modules/`、`app/main.py`、入口脚本等），确认当前真实行为与边界，而不是凭通用经验推断。
4. **文档与代码冲突时**：以 **代码当前行为** 为准并提醒用户；若应改行为，以 **文档或用户明确说明** 为准并同步更新 `doc/`（保持简略）。

## 禁止与建议

- **禁止**：在未浏览 `doc/`（或根 `README`）、未检索相关代码前，自行「发明」完整业务流程或字段含义。
- **建议**：若 `doc/` 无对应说明，完成实现后在 `doc/` **补一页以内** 说明，便于后续需求对齐（与 `doubao-spider.mdc` 文档原则一致）。

## 与其它规则的关系

- UI 细节与选择器：仍遵循 **`doubao-spider.mdc`**。
- 格式与类型：遵循 **`code-style.mdc`**。
- 架构与领域边界：遵循 **`architecture-patterns.mdc`**、**`ddd-domain.mdc`**。
