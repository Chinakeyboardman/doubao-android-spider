---
description: DDD 限界上下文、聚合、领域模型与分层在爬虫项目中的落地要求
globs: "**/*.{py,md}"
alwaysApply: false
---

# DDD 与领域模型规范

> **目标**：在 **单体** 中保持清晰的 **领域边界** 与 **模型纯度**，便于测试与未来按限界上下文拆服务。不要求一次性重构全库；**新增或大面积修改模块** 时应按本规范对齐。

## 限界上下文（Bounded Context）

为本项目建议的 **语境划分**（名称可随演进调整，但职责需单一）：

| 上下文 | 职责 | 典型载体（当前/预期） |
|--------|------|------------------------|
| **设备自动化** | 连接设备、封装 uiautomator2/adb、与系统层交互 | `DeviceManager`、`DoubaoApp`（启动/窗口粗检） |
| **会话与交互** | 聊天页判定、输入发送、等待回复 | `chat_window`、`input_output` |
| **内容抓取** | 回复文本/富文本、卡片结构解析 | `response_capture` |
| **商品采集** | 列表/详情页采集、卡片点击流程 | `product_*`、`card_click_worker` |
| **编排（应用层）** | 用例步骤组合、事务边界（一次 run）、不包含 UI 细节 | `DoubaoSpider`、入口 `run.py` |

- **禁止**：在「设备自动化」里写商品字段解析；在「商品采集」里直接散落裸 `adb shell`（应经设备外观）。
- **跨上下文**：通过 **应用服务** 编排或 **明确 DTO** 传递，不共享「大而全」的全局可变状态。

## 分层与依赖方向

- **领域层（核心）**：**设备会话、消息、商品卡片/链接** 等领域的 **实体规则、不变量**；尽量不依赖 uiautomator2 具体 API（通过端口/接口由基础设施实现）。
- **应用层**：**用例**（SendPrompt、CaptureReply、ClickCards）；协调领域对象与基础设施；**无** XPath/resource-id 细节。
- **基础设施层**：u2、文件系统、HTTP、mitm、配置读取；实现领域定义的 **仓储/网关接口**（若已抽象）。
- **依赖规则**：**内层不依赖外层**；外层依赖内层接口。新增代码避免 `from app.modules.response_capture import ...` 被 `utils` 反向引用等 **循环依赖**。

## 模型元素

- **实体（Entity）**：有 **标识（id）** 且生命周期内可变，同一 id 即同一对象（如「一次抓取任务」「一条会话回合」若需要追踪）。
- **值对象（Value Object）**：**不可变**、无独立身份、由属性整体决定相等性（如 `CardLink`、`Bounds`、`PromptText`）；用 `dataclass(frozen=True)` 或 `NamedTuple` 等表达。
- **聚合（Aggregate）**：**一致性边界**；外部只能通过 **聚合根** 修改内部状态；聚合间引用 **仅保存 id**，不持有对方可变引用。
- **领域服务**：当规则 **不属于单一实体** 时（如「判断当前 UI 是否允许发送」若规则复杂），用 **无状态领域服务函数/类**，仍保持不依赖基础设施细节（或仅依赖已注入的端口）。

## 防腐层（Anti-Corruption Layer）

- **外部系统**：豆包 App UI、第三方 JSON、抓包原始结构均为 **外部模型**。
- **要求**：在边界处 **映射** 为内部 **值对象/DTO**（如卡片列表、链接 URL），**禁止** 把外部 dict key 深层透传到编排层各处。
- **解析逻辑**：集中在单一模块（如 `response_capture` 内子函数），对外返回 **已校验** 的领域或应用 DTO（与 `code-style.mdc` 一致）。

## 应用服务与领域事件（可选）

- **应用服务** 方法对应 **一个用例**（如 `run_capture_flow`），参数显式、返回值类型明确。
- 若引入 **领域事件**（如 `ReplyCaptured`）：在 **同一进程** 内可用简单发布/订阅或队列；跨服务时须 **持久化 + 至少一次投递**（与 `microservices.mdc` 对齐）。

## 文档与命名

- 新限界上下文或聚合变更时，在 `doc/` **追加简短说明**（一页内）：边界、对外 DTO、不变量。
- 模块/包命名体现 **业务语言**（如 capture、card、device），避免 `Manager2`、`HelperUtil` 等无领域含义堆砌。

## 与其它规则的关系

- **微服务拆分** 时，服务边界优先与 **限界上下文** 对齐：见 **`microservices.mdc`**。
- Python 格式与入参出参严格模式：见 **`code-style.mdc`**。
- UI 与选择器细节：见 **`doubao-spider.mdc`**。
